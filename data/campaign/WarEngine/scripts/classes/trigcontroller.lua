---===TrigController===---
-- includes
objects.Init()

-- declaring
dbg.Print("| Requiring 'TrigController' class.")
TrigController = objects.Class("TrigController")

-- public methods
function TrigController:initialize(name, props, trigPos, trigProps)
	-- i really don't know, what must be there
end